.env
.env.local
*.env

.DS_Store
node_modules/
.netlify/

# Tooling local, pas à committer
.claude/
deno.lock

# Agent RH local : plists launchd weekly/monthly avec chemins absolus de la
# machine de David, non généralisables. Créés à la main après clone.
# Ancré à la racine pour ne pas masquer agent/launchd/ qui contient le plist
# poll-loop committé comme template (chemins absolus à adapter).
/launchd/
